{
  "name": "@email-wallet/subgraph",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ zk-email/email-wallet",
    "create-local": "graph create --node http://localhost:8020/ zk-email/email-wallet",
    "remove-local": "graph remove --node http://localhost:8020/ zk-email/email-wallet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zk-email/email-wallet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
